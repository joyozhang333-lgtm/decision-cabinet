# 安全策略

## 报告漏洞

请不要在公开 Issue 中发布 API key、数据库、私人决策记录或可直接利用的漏洞细节。请通过 GitHub Security Advisories 的 **Report a vulnerability** 私下报告。

报告请包含受影响版本、复现步骤、影响范围和建议修复方式。维护者会尽快确认。

## 运行边界

- `.env` 与 `*.db` 默认不进入版本控制；请将权限设为 `0600`。
- 对外 `/api/v1/*` 只有设置 `CABINET_EXTERNAL_API_KEY` 后才启用（兼容旧名 `CABINET_API_KEY`）。
- 私有 `/api/*` 默认只允许本机 loopback。经反向代理、局域网或公网访问时必须设置 `CABINET_UI_API_KEY`，并使用 HTTPS；不要在共享主机上使用弱密钥。
- 生产环境应配置反向代理限流、并发上限、日志脱敏与数据库备份。
- 决策档案和历史记录默认不发送给外部模型；只在用户主动勾选的本轮附带。
- 不要把真实证券账户、身份证明或高敏感私人信息直接发送给第三方模型。
- 任何外部知识抓取都应做来源白名单、内容长度限制和提示注入隔离。

## 支持版本

当前只维护最新 `main` 与最近的正式版本。
