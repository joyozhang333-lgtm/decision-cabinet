# 参与贡献

感谢你让决策内阁更真实、更可检验。欢迎提交代码、知识卡、测试、文档和产品建议。

## 本地开发

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e '.[dev]'
pytest -q

cd web
npm ci
npm test
npm run build
npm run build:demo
```

## 新增知识卡

在 `cabinet/resources/knowledge/<domain>/<id>.md` 新建文件，必须包含：`ID`、`领域`、`传统`、`标签`、`摘要`、`关键问题`、`方法`、`边界`、`来源`。

来源优先级：

1. 法律法规、监管机构、交易所、公司法定披露、官方统计；
2. 原始论文、标准组织、作者或机构的一手材料；
3. 高质量二手综述；
4. 社交媒体和无来源转述不能作为关键事实依据。

现代著作只写方法摘要，不复制长段原文。经典文本若引用，必须写明版本和定位。任何解释都要明确它是解释，不冒充原文唯一含义。

## 金融与投资内容

提交股票、基金、宏观或估值相关改动时，必须检查：

- 是否明确数据时点、报告期、币种和口径；
- 是否把事实、公司指引、市场预期和作者判断分开；
- 是否包含反证、风险预算、流动性与退出条件；
- 是否避免收益承诺、确定性目标价或未经授权的个性化买卖指令；
- 是否链接到原始披露，而不是只链接媒体转述。

## 顾问与经典

- 不得声称人物型顾问得到真实人物授权。
- 不得伪造逐字原话、经文、偈颂或出处。
- 人物方法必须允许被批评，并清楚写出适用边界。
- 顾问不能替用户做最终决定。

## 提交前 review

- 产品逻辑：流程是否真的帮助用户看清选择、代价与演化？
- 个性化与隐私：fixture、截图、README 是否含真实个人资料？
- 安全边界：是否可能被误解为收益、医疗、法律或命运保证？
- 回归风险：离线 fallback、外部 provider、SQLite 与 API 是否仍兼容？
- 视觉一致性：移动端、空状态、错误状态是否可用？
- 测试缺口：新增分支是否有稳定、零网络测试？

PR 请说明问题、方案、验证结果和已知限制。重大知识库变更请列出来源链接。
